          ┌─────────────────────────────┐
          │       Cliente / App         │
          └──────────────┬─────────────┘
                         │
                         ▼
          ┌─────────────────────────────┐
          │      Servidor Web / HTTP    │
          │  (Tomcat, Jetty, Undertow) │
          └──────────────┬─────────────┘
                         │
                         ▼
          ┌─────────────────────────────┐
          │     Container de Servlets   │
          │  (Gerencia ciclo de vida)  │
          └──────────────┬─────────────┘
                         │
        ┌────────────────┴─────────────────┐
        │                                  │
        ▼                                  ▼
┌─────────────────────┐            ┌─────────────────────┐
│ Spring MVC Dispatcher│           │ RESTEasy Reactive    │
│    Servlet           │           │   (JAX-RS)          │
└──────────┬───────────┘           └──────────┬───────────┘
           │                                   │
           ▼                                   ▼
┌─────────────────────────┐         ┌─────────────────────────┐
│ @RestController /       │         │ @Path / @GET / @POST    │
│ @GetMapping / @PostMapping │      │ @Produces / @Consumes   │
└──────────┬───────────────┘        └──────────┬──────────────┘
           │                                   │
           ▼                                   ▼
      Resposta HTTP                        Resposta HTTP
      (JSON, HTML, etc)                    (JSON, HTML, etc)


🔹 Observações:

- Servidor Web

Spring Boot: Tomcat, Jetty ou Undertow embutido
Quarkus: Undertow ou Vert.x

- Container de Servlets: Gerencia os servlets, filtros e listeners, garantindo o ciclo de vida da aplicação

- Dispatcher / REST layer
Spring Boot: DispatcherServlet abstrai os servlets e chama os métodos anotados com Spring MVC
Quarkus: RESTEasy Reactive processa requisições via JAX-RS

- Código do desenvolvedor
Spring Boot: @RestController + @GetMapping / @PostMapping
Quarkus: @Path + @GET / @POST + @Produces/@Consumes